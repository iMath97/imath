var mySwiper = new Swiper ('.swiper-container', {
    // Optional parameters
    direction: 'horizontal',
    loop: true,
    centeredSlides: true,
    effect: 'fade',
    
    autoplay: {
        delay: 2500,
        disableOnInteraction: false,
      },
  });